IMPORTANT, at login screen, one must do CTRL+ALT+F2 to change the password
to change later and not on a VM, put true
d-i partman-crypto/erase_data boolean false

note: the script is not intended for a VM long term so open-vm-tools open-vm-tools-desktop are not included
--> use temporary firewall rules. Use the NAT network card and add the checksum and url as is explained below.

----------

http://localhost:8000/preseed.cfg
IP is 192.168.200.1 and works if VM is in NAT network. NAT gives internet access.

cd "C:\Users\SESA780079\Downloads\Virtual Machines\preseed\preseed_script>"
python3 -m http.server 8000

python can be installed from the Windows store


----------------------------
In the VMware boot screen of Debian

Highlight Advanced options > Automated install.

Press TAB (or e in UEFI) to edit the boot parameters.

it worked but one must type e when selecting automated install

Append the path to your preseed file. If using a local web server:
add this after critical:
checksum=1952a37aeed4b4bab1dc7a2aeec96d60 url=http://192.168.243.1:8000/preseed.cfg

then press F10

the SHA is created like this:
(Get-FileHash .\preseed.cfg -Algorithm MD5).Hash.ToLower()
Copy the Hash (a long string of letters and numbers).

that it worked is shown by the request received by the http server

-------------------------------------
# Fix the USB stick

in gparted
2 partitions one ext4 one in FAT32 to copy files

Select gpt and click Apply. (This is essential for modern UEFI systems).

2. Create the Two Partitions
Partition 1 (The Installer):

Right-click the unallocated space and select New.

Size: 2000 MiB (2GB is plenty for a NetInst).

File system: fat32.

Label: DEBIAN_BOOT.

Partition 2 (The Data):

Right-click the remaining space and select New.

File system: fat32 (or ext4 if you prefer).

Label: INSTDATA.

3. Set the Boot Flag esp on partition 1

----------------------------------------------------
copy the .iso with the debian installer

sudo mkdir -p /mnt/debian_iso
sudo mount -o loop /path/to/your/debian-netinst.iso /mnt/debian_iso
sudo mkdir -p /mnt/usb_boot
sudo mount /dev/sdc1 /mnt/usb_boot
sudo cp -a /mnt/iso/. /mnt/usb_boot/
---------------------
install grub

# Install the necessary binaries on your host system
sudo apt install grub-efi-amd64-bin shim-signed

# Install GRUB to the USB (Partition 1)
sudo grub-install --target=x86_64-efi \
             --efi-directory=/media/user/DEBIAN_BOOT \
             --boot-directory=/media/user/DEBIAN_BOOT/boot \
             --removable --uefi-secure-boot

3. The Magic: /boot/grub/grub.cfg
GRUB itself won't know to start the Debian installer unless you tell it what to do. You need to create a text file at /boot/grub/grub.cfg on that USB stick.

This configuration is what "triggers" the installer and passes your preseed file location. It looks like this:

set timeout=5
set default=0

menuentry "Install Debian (Automated)" {
    linux /install.amd/vmlinuz auto=true priority=critical preseed/file=/hd-media/sdc2/preseed.cfg --- quiet
    initrd /install.amd/initrd.gz
}

----------------------------------------------------------------------------------------
menuentry "Automated Debian Install" {
    # Specify the path to the kernel and ramdisk (copied from the ISO)
    set linux_path="/install.amd/vmlinuz"
    set initrd_path="/install.amd/initrd.gz"

    linux $linux_path auto=true priority=critical preseed/file=/hd-media/sdb2/preseed.cfg --- quiet
    initrd $initrd_path
}

4. The File Structure Check
After running the command or copying from the ISO, your Partition 1 (DEBIAN_BOOT) should look like this:

Plaintext
/ (Root of Partition 1)
├── EFI/
│   └── BOOT/
│       ├── BOOTX64.EFI  <-- This is the Shim (signed)
│       ├── grubx64.efi  <-- This is GRUB (signed)
│       └── mmx64.efi    <-- MokManager (for managing keys)
├── boot/
│   └── grub/
│       └── grub.cfg     <-- YOUR custom config file
└── install.amd/         <-- Kernel and Initrd from the ISO
-------------------------------------
fix global protect

copy with Firefox the chain certificate of gp.schneider-electric.com and install it
sudo mv gp-schneider-electric-com-chain.pem /usr/local/share/ca-certificates/schneider/
sudo update-ca-certificates

sudo apt update
sudo apt install gp-saml-gui
export PATH=$PATH:/usr/sbin
export REQUESTS_CA_BUNDLE=/etc/ssl/certs/ca-certificates.crt
gp-saml-gui --portal gp.schneider-electric.com --clientos=Windows --allow-insecure-crypto

echo PXi92W5qiQ2N+tJCwBPyir5X0o1TaqKZ/VTkMj9/VGhu6p7zYd18tVHNb47nG9BmYHDwtg== | \
sudo openconnect --protocol=gp \
--useragent='PAN GlobalProtect' \
--allow-insecure-crypto \
--user='SESA780079@eur.gad.schneider-electric.com' \
--os=win \
--usergroup=portal:prelogin-cookie \
--passwd-on-stdin gp.schneider-electric.com

final that works:
export REQUESTS_CA_BUNDLE=/etc/ssl/certs/ca-certificates.crt
sudo -E  gp-saml-gui --no-verify --gateway --clientos=Windows --allow-insecure-crypto --sudo-openconnect europe-g1.gp.schneider-electric.com -- --allow-insecure-crypto --useragent='PAN GlobalProtect'



