set timeout=3
set default=0

menuentry "Standard Debian Install" {
    search --no-floppy --file /install.amd/vmlinuz --set=root

    linux /install.amd/vmlinuz auto=true priority=critical \
    preseed/file=/cdrom/preseed.cfg \
    --- quiet

    initrd /install.amd/initrd.gz
}
